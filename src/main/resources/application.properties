spring.application.name=Interview-Simulator

spring.datasource.url=${DATABASE_URL}

spring.jpa.hibernate.ddl-auto=update

server.port=${PORT:8080}

gemini.ai.key=${GEMINI_API_KEY}
groq.ai.key=${GROQ_API_KEY}